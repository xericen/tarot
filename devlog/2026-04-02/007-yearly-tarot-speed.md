# 연간 타로 AI 분석 시간 단축

- **ID**: 007
- **날짜**: 2026-04-02
- **유형**: 기능 추가

## 작업 요약
연간 타로(page.page.tarotw) API에서 4계절 운세를 각각 개별 Gemini API 호출(4회)로 처리하던 것을, 단일 프롬프트로 합쳐 1회 호출로 변경. 응답은 JSON 배열로 파싱하여 기존 프론트엔드 데이터 구조와 호환.

## 변경 파일 목록

### page.page.tarotw (연간 타로)
- `api.py`: `import json` 추가, 4계절 카드 정보를 하나의 프롬프트에 통합, JSON 배열 응답 파싱 로직 추가, 에러 시 개별 계절 fallback 메시지 유지
