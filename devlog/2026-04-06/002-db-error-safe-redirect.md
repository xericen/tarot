# DB 오류 시 로그인 화면으로 안전한 리다이렉트

- **ID**: 002
- **날짜**: 2026-04-06
- **유형**: 버그 수정

## 작업 요약
`member.py` 컨트롤러의 DB 검증 로직을 견고하게 수정. 기존에는 `except` 블록에서 세션만 클리어하고 이후 별도 조건문으로 401을 반환했으나, DB 연결 오류·모델 로딩 실패·int 변환 실패 등 모든 예외 상황에서 즉시 `wiz.response.status(401)` 호출하여 로그인 화면으로 리다이렉트하도록 변경.

## 변경 파일 목록
### 백엔드 (Controller)
- `src/controller/member.py`: except 블록에서 세션 클리어 후 즉시 401 반환하도록 수정
